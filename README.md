# Purchase-Predictor
Machine learning-based Flask web app that predicts customer subscription behavior using models like SVM, Random Forest, Logistic Regression, and Gradient Boosting. Automatically selects the best model based on ROC-AUC score and provides real-time predictions with confidence values.
